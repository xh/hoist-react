---
phase: 01-current-state-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/planning/data2/COPY-VS-REUSE.md
autonomous: true
requirements: [INV-02]
must_haves:
  truths:
    - "A reader can see, at each pipeline transition, whether the datum is the same JS object reference or a new copy"
    - "The map covers all five transitions: raw object -> StoreRecord -> ViewResult rows -> grid store records -> AG Grid nodes"
    - "Every copy-vs-reference claim cites a source file:line"
    - "The map identifies where aggregate/group rows are genuinely new allocations vs. where leaf data is shared"
  artifacts:
    - path: "docs/planning/data2/COPY-VS-REUSE.md"
      provides: "Copy-vs-reuse map across the full data pipeline with object-identity reasoning and source citations"
      min_lines: 120
      contains: "StoreRecord"
  key_links:
    - from: "docs/planning/data2/COPY-VS-REUSE.md"
      to: "data/StoreRecord.ts"
      via: "cited file:line for raw-ref vs inner-data-object"
      pattern: "StoreRecord\\.ts:"
    - from: "docs/planning/data2/COPY-VS-REUSE.md"
      to: "data/cube/View.ts"
      via: "cited file:line for ViewRowData -> store load object identity"
      pattern: "View\\.ts:"
---

<objective>
Produce the copy-vs-reuse map (INV-02): a precise, source-cited accounting of where the same datum
is shared by reference vs. copied into a new allocation, at every transition in the current data
pipeline. This resolves one of the two explicit empirical unknowns carried into Phase 1 and is a
direct input to the heap-attribution work in Phase 2.

Purpose: The "memory-multiplication problem" is central to the Data 2.0 question. Before any heap
can be attributed by layer, we must know exactly where data is duplicated. This is reasoning about
object identity from source, not measurement (measurement is Phase 2/3).

Output: docs/planning/data2/COPY-VS-REUSE.md
</objective>

<execution_context>
@/Users/amcclain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amcclain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/planning/data2/KICKOFF-VALIDATION.md
@docs/planning/data2/validation/A-store-cube-view.md

# Source files this plan must read in detail (do not guess - read them):
@data/StoreRecord.ts
@data/Store.ts
@data/cube/View.ts
@data/cube/ViewRowData.ts
@data/cube/row/LeafRow.ts
@data/cube/row/AggregateRow.ts
@data/cube/row/BaseRow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace object identity through the source and assemble the copy-vs-reuse map</name>
  <files>docs/planning/data2/COPY-VS-REUSE.md</files>
  <action>
    Read the source files listed in `<context>` plus `data/cube/Cube.ts` and
    `data/cube/aggregate/AggregationContext.ts`. Use the hoist-react MCP tools to confirm signatures
    where helpful (`hoist-get-symbol StoreRecord`, `hoist-get-members View`, `hoist-get-symbol ViewRowData`).
    DO NOT guess at object-identity behavior - read the constructors and assignment sites.

    Trace and document each of the FIVE pipeline transitions, answering "same reference or new
    object?" with a cited file:line for each:

    1. Raw JSON object -> StoreRecord. Read `StoreRecord` constructor and `Store.createRecord()`
       / the `processRawData` call site. Confirm the documented "keeps a reference to the raw object
       AND a new inner data object" claim (PROJECT.md data-flow section). Is `data` frozen
       (`freezeData`)? Note the Cube's internal store sets `freezeData: false` (per
       A-store-cube-view.md 3.6) - explain what that allows (in-place leaf mutation).

    2. StoreRecord (in Cube's internal store) -> ViewResult rows (leaf path). Read `LeafRow` and
       `ViewRowData`. Determine whether a leaf `ViewRowData` shares field values with the original
       leaf `StoreRecord.data` or copies them. This is open question A-1/A-2 in
       A-store-cube-view.md - resolve it from source.

    3. View aggregation -> aggregate/group rows. Read `AggregateRow` and `View.generateRows()` /
       `dataOnlyUpdate()`. Confirm aggregate rows are genuinely new allocations (not shared),
       and document the `dataOnlyUpdate()` in-place mutation fast path (A-store-cube-view.md 3.8)
       and why it forbids `reuseRecords: true` on connected stores (View.ts ~554-558).

    4. ViewResult rows -> grid store records. Read `View.loadStores()` -> `Store.loadData()` ->
       `Store.createRecord()`. Determine whether the `ViewRowData` objects placed in `view.result.rows`
       are the SAME objects loaded into the connected store's `StoreRecord.data`, or copied. This is
       the primary aggregated-row copy question (open question A-1).

    5. Grid store records -> AG Grid nodes. AG Grid copies row data into its own internal row nodes
       keyed by `getRowId: ({data}) => data.agId` (B-grid-mobx.md section 2). Document that AG Grid
       maintains its own node structure referencing `StoreRecord.data` (or `agId`); cite Grid.ts where
       `getRowId` and `applyTransaction` pass record data. Note this transition crosses into a
       third-party library - be explicit that AG Grid internal node memory is opaque-but-real and
       flag it as a heap-attribution boundary for Phase 2.

    Structure the document as:
    - Intro framing the memory-multiplication problem (1 short paragraph, generic terms only).
    - A summary table: Transition | Copied or Referenced | What is new | Source cite.
    - A per-transition section with the detailed reasoning and file:line citations.
    - A "representations of one datum" walkthrough: follow a single leaf field value from JSON
      string through to an AG Grid cell, listing every concurrent in-memory representation.
    - An explicit "open / could-not-determine-from-source" list if any identity question cannot be
      settled by reading alone (these become Phase 2 measurement targets).

    OPEN-REPO RULE: no private client/customer names. Allowed proper names: Hoist, Toolbox, JobSite.
    Use generic terms ("a client app", "portfolio app") otherwise. This doc is committed to a public repo.

    Use a plain spaced hyphen " - ", never an em dash, in all prose.
  </action>
  <verify>
    `test -f docs/planning/data2/COPY-VS-REUSE.md` and the file contains a summary table covering all
    five transitions. Confirm at least one `StoreRecord.ts:` and one `View.ts:` line citation are
    present (`grep -E "StoreRecord\.ts:|View\.ts:" docs/planning/data2/COPY-VS-REUSE.md`). Run the
    open-repo guard if available: `~/.claude/projects/-Users-amcclain-dev-hoist-react/client-name-guard/`
    scanner against the new file; zero forbidden names.
  </verify>
  <done>
    COPY-VS-REUSE.md exists, documents all five transitions with copied-vs-referenced verdicts,
    every verdict carries a source file:line citation, the single-datum representation walkthrough is
    present, and the doc contains no forbidden names.
  </done>
</task>

</tasks>

<verification>
- Document covers all five pipeline transitions (raw -> StoreRecord -> ViewResult rows -> grid store
  records -> AG Grid nodes).
- Each copy-vs-reference verdict is backed by a file:line citation into hoist-react source.
- The two A-store-cube-view.md open questions on leaf and aggregate-row identity are resolved (or
  explicitly flagged as measurement-only if source is inconclusive).
- No private client names.
</verification>

<success_criteria>
INV-02 satisfied: a reader can determine, at every pipeline transition, where data is copied vs.
referenced, grounded in source citations, with the heap-attribution boundaries flagged for Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-current-state-inventory/01-01-SUMMARY.md`
</output>
