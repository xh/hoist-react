---
phase: 01-current-state-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/planning/data2/MOBX-GRANULARITY.md
autonomous: true
requirements: [INV-03]
must_haves:
  truths:
    - "A reader can tell whether each reaction in the View.result -> Store -> GridModel -> AG Grid path fires per-record or per-batch"
    - "The doc identifies exactly where the synchronous applyTransaction is driven and from which reaction"
    - "The mounted-component reaction lifecycle is documented (reactions live in GridLocalModel.onLinked, active only while mounted)"
    - "The cube -> view imperative push vs. the View.result MobX seam distinction is made explicit with citations"
  artifacts:
    - path: "docs/planning/data2/MOBX-GRANULARITY.md"
      provides: "MobX reaction-granularity trace of the full update path with batch-vs-record level and lifecycle"
      min_lines: 120
      contains: "applyTransaction"
  key_links:
    - from: "docs/planning/data2/MOBX-GRANULARITY.md"
      to: "cmp/grid/Grid.ts"
      via: "cited file:line for dataReaction and applyTransaction"
      pattern: "Grid\\.ts:"
    - from: "docs/planning/data2/MOBX-GRANULARITY.md"
      to: "data/cube/View.ts"
      via: "cited file:line for View.result observable.ref and noteCubeUpdated"
      pattern: "View\\.ts:"
---

<objective>
Produce the MobX reaction-granularity trace (INV-03): a precise, source-cited account of how an
update propagates along `View.result -> Store -> GridModel -> AG Grid`, classifying each hop as
record-level vs. batch-level, pinpointing where the synchronous `applyTransaction` is driven, and
documenting the mounted-component reaction lifecycle. This resolves the second of the two explicit
empirical unknowns carried into Phase 1.

Purpose: "Real-time" throughput is bounded by how reactions fire and how transactions are applied.
Knowing the granularity (whole-RecordSet reference vs. per-record) and the synchronous, mounted-only
nature of the AG Grid reactions is what tells Phase 2 where the main-thread cost lives.

Output: docs/planning/data2/MOBX-GRANULARITY.md
</objective>

<execution_context>
@/Users/amcclain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amcclain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/planning/data2/KICKOFF-VALIDATION.md
@docs/planning/data2/validation/B-grid-mobx.md

# Source files this plan must read in detail (do not guess - read them):
@cmp/grid/Grid.ts
@cmp/grid/GridModel.ts
@data/Store.ts
@data/cube/View.ts
@data/cube/Cube.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace the reaction path and classify granularity end to end</name>
  <files>docs/planning/data2/MOBX-GRANULARITY.md</files>
  <action>
    Read the source files in `<context>`. Use the hoist-react MCP tools to confirm member/decorator
    details (`hoist-get-members GridModel`, `hoist-get-symbol View`, `hoist-search-symbols "GridLocalModel"`).
    DO NOT guess at decorators or reaction wiring - read the `@observable`/`@observable.ref` declarations
    and `addReaction` call sites.

    Document each hop of the path with a granularity verdict (record-level vs. batch/reference-level)
    and a file:line citation:

    1. Cube -> View. Confirm this is IMPERATIVE push, not MobX observation: `Cube.loadDataAsync` /
       `updateDataAsync` iterate `_connectedViews` calling `view.noteCubeLoaded()` / `noteCubeUpdated()`
       (KICKOFF-VALIDATION 2; A-store-cube-view 3.3). Cite Cube.ts. Make explicit that there is no MobX
       observable at the cube-to-view boundary.

    2. View.result -> downstream. Confirm `view.result` is `@observable.ref` of `ViewResult {rows, leafMap}`
       (View.ts). Verify the exact decorator from source (resolve B-grid-mobx open question #1). State
       that this is the MobX seam and that it fires at the WHOLE-result reference level (a new ViewResult
       object), not per-row - this is the integration seam for any Data 2.0 engine.

    3. View -> Store. `loadStores()` -> `store.loadData()` produces a new `store._filtered` RecordSet
       (`@observable.ref`, Store.ts ~308). Document that Store observability is at the RecordSet
       reference level (whole dataset), not per-record (B-grid-mobx section 3). Confirm what triggers
       `rebuildFiltered()` (a reaction in the Store constructor on `this.filter`; resolve B-grid-mobx
       open question #2 by reading the Store constructor).

    4. Store -> GridModel/AG Grid. Read `Grid.ts` `GridLocalModel`. Document the full reaction set
       registered in `onLinked()` (dataReaction, selectionReaction, sortReaction, groupReaction,
       columnsReaction, columnStateReaction, rowHeightReaction, validationDisplayReaction) with what
       each tracks and its effect (B-grid-mobx section 3 table) - cite Grid.ts line ranges.

    5. The applyTransaction hop. Pin down `syncData()` -> `genTransaction()` (three-way diff by record
       identity) -> SYNCHRONOUS `agApi.applyTransaction()` (Grid.ts ~693), with full-replacement
       (`updateGridOptions({rowData})`) only when `prevCount === 0`. Cite the exact lines. State clearly:
       no `applyTransactionAsync`, no explicit batching layer; the only coalescing is implicit MobX
       action batching.

    Then add two synthesis sections:
    - "Granularity verdict" - a table: Hop | Record-level or Batch/Reference-level | Mechanism | Cite.
      The headline finding: observation is at whole-RecordSet/whole-ViewResult reference granularity,
      while the AG Grid transaction itself is a per-record diff applied synchronously in one call.
    - "Mounted-component reaction lifecycle" - explain that the AG-Grid-driving reactions live in
      `GridLocalModel` and are registered in `onLinked()`, so they are active ONLY while the grid
      component is mounted; `GridModel` itself holds essentially one reaction (an editing debounce,
      GridModel.ts ~752). Cite both. Note the implication: an unmounted grid does not apply
      transactions, and reaction (re)registration happens on mount.

    Close with an explicit list of any source-inconclusive points (carry to Phase 2 as measurement
    targets), e.g. whether MobX action batching actually coalesces rapid sequential updates in practice.

    OPEN-REPO RULE: no private client/customer names. Allowed proper names: Hoist, Toolbox, JobSite.
    Use a plain spaced hyphen " - ", never an em dash, in all prose.
  </action>
  <verify>
    `test -f docs/planning/data2/MOBX-GRANULARITY.md`. Confirm it contains the per-hop granularity
    table and the lifecycle section. Confirm citations present:
    `grep -E "Grid\.ts:|View\.ts:|Store\.ts:" docs/planning/data2/MOBX-GRANULARITY.md`. Confirm the
    word `applyTransaction` appears and is described as synchronous. Run the open-repo guard scanner
    against the new file; zero forbidden names.
  </verify>
  <done>
    MOBX-GRANULARITY.md exists; every hop of View.result -> Store -> GridModel -> AG Grid has a
    record-vs-batch verdict with a source citation; the synchronous applyTransaction site is pinpointed;
    the mounted-only reaction lifecycle is documented; no forbidden names.
  </done>
</task>

</tasks>

<verification>
- Each hop classified record-level vs. batch/reference-level with a citation.
- The synchronous applyTransaction site and the absence of async/explicit batching are documented.
- The mounted-component reaction lifecycle (GridLocalModel.onLinked) is documented with citations.
- The imperative cube->view push vs. the View.result MobX seam distinction is explicit.
- No private client names.
</verification>

<success_criteria>
INV-03 satisfied: the MobX reaction granularity of the full update path is documented as record-level
vs. batch-level, the synchronous applyTransaction driver is identified, and the mounted-component
reaction lifecycle is captured - all source-cited.
</success_criteria>

<output>
After completion, create `.planning/phases/01-current-state-inventory/01-02-SUMMARY.md`
</output>
