---
phase: 01-current-state-inventory
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - docs/planning/data2/ARCHITECTURE.md
autonomous: true
requirements: [INV-01]
must_haves:
  truths:
    - "A reader gets one authoritative current-state architecture document covering Store / Cube / View / GridModel and the end-to-end data flow"
    - "The document contains Mermaid diagrams (at least: a component/data-flow diagram and a reactivity/sequence diagram)"
    - "The document absorbs and supersedes the prior validation notes (KICKOFF-VALIDATION + A/B/C) - a reader needs only this doc"
    - "The document names the concrete instrumentation points the Phase 2 harness will hook (boundaries where heap and timing are attributable)"
    - "The copy-vs-reuse, MobX-granularity, and transport findings (INV-02/03/04) are integrated by reference and summary, not left as separate orphans"
  artifacts:
    - path: "docs/planning/data2/ARCHITECTURE.md"
      provides: "Authoritative current-state architecture doc with Mermaid diagrams, instrumentation points, and integrated findings"
      min_lines: 200
      contains: "```mermaid"
  key_links:
    - from: "docs/planning/data2/ARCHITECTURE.md"
      to: "docs/planning/data2/COPY-VS-REUSE.md"
      via: "linked + summarized (INV-02 integration)"
      pattern: "COPY-VS-REUSE"
    - from: "docs/planning/data2/ARCHITECTURE.md"
      to: "docs/planning/data2/MOBX-GRANULARITY.md"
      via: "linked + summarized (INV-03 integration)"
      pattern: "MOBX-GRANULARITY"
    - from: "docs/planning/data2/ARCHITECTURE.md"
      to: "docs/planning/data2/TRANSPORT-INVENTORY.md"
      via: "linked + summarized (INV-04 integration)"
      pattern: "TRANSPORT-INVENTORY"
---

<objective>
Produce the authoritative current-state architecture document (INV-01): a single source-grounded
document covering Store / Cube / View / GridModel and the end-to-end data flow, with Mermaid diagrams,
that absorbs and supersedes the prior validation notes and integrates the three Wave 1 deliverables
(copy-vs-reuse, MobX granularity, transport inventory). It also names the concrete instrumentation
points the Phase 2 harness will hook - the bridge into Phase 2.

Purpose: This is the keystone deliverable every later phase depends on. After this exists, a reader
should be able to understand the current data layer from this one document (with the three companion
deep-dives linked for detail) and the validation notes become historical backing.

Output: docs/planning/data2/ARCHITECTURE.md
</objective>

<execution_context>
@/Users/amcclain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amcclain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/planning/data2/KICKOFF-VALIDATION.md
@docs/planning/data2/validation/A-store-cube-view.md
@docs/planning/data2/validation/B-grid-mobx.md
@docs/planning/data2/validation/C-real-usage.md

# Wave 1 deliverables this plan integrates (read the produced docs):
@docs/planning/data2/COPY-VS-REUSE.md
@docs/planning/data2/MOBX-GRANULARITY.md
@docs/planning/data2/TRANSPORT-INVENTORY.md

# Authoritative source docs - read via MCP for accurate API framing:
# hoist-read-doc data/README.md ; hoist-read-doc data/cube/README.md ; hoist-read-doc cmp/grid/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the architecture narrative with Store/Cube/View/GridModel and the two production wiring patterns</name>
  <files>docs/planning/data2/ARCHITECTURE.md</files>
  <action>
    Read all `<context>` inputs, including the three Wave 1 deliverables (COPY-VS-REUSE.md,
    MOBX-GRANULARITY.md, TRANSPORT-INVENTORY.md). Use the hoist-react MCP doc tools to ground the API
    framing: `hoist-read-doc data/README.md`, `hoist-read-doc data/cube/README.md`,
    `hoist-read-doc cmp/grid/README.md`. DO NOT restate guesses - lean on source-cited validation notes.

    Write the front matter and core artifact sections of ARCHITECTURE.md:
    - A short preamble stating this doc supersedes/absorbs the validation notes (KICKOFF-VALIDATION +
      A/B/C), which remain as historical backing. State the source revision/date.
    - "Core artifacts" sections for Store, Cube, View, GridModel - each: responsibility, key API,
      what it observes/exposes, and the validated corrections (no built-in weighted-average; cube wraps
      a Store; View feeds one OR MORE stores; chooser is column-state, filter is store-level;
      synchronous applyTransaction; reactions live in the mounted grid component). Pull the corrected
      facts from the validation notes - cite source file:line where a claim is load-bearing.
    - "The shared-store contract" section - why the Store is an AG-Grid-independent substrate that
      charts/toolbars/filters consume, and why this is central to Hoist's value (and the tension with
      "just use AG Grid features"). This frames the seam later phases protect.
    - "Two production wiring patterns" section - (a) declarative `createView({connect:true})` +
      `setStores`; (b) manual MobX reaction on `cube.records` -> `executeQuery()` -> `gridModel.loadData()`.
      Describe both generically with abstracted code (NO app names beyond Toolbox/JobSite), noting when
      each is preferred (C-real-usage 5.1).

    Maintain the correct terminology glossary from KICKOFF-VALIDATION. Use generic terms for any
    non-public app. Plain spaced hyphen " - ", never an em dash.
  </action>
  <verify>
    `test -f docs/planning/data2/ARCHITECTURE.md` and it contains sections for Store, Cube, View, and
    GridModel (`grep -E "Store|Cube|View|GridModel" docs/planning/data2/ARCHITECTURE.md`). Both wiring
    patterns described. (Diagrams + integration added in Task 2.)
  </verify>
  <done>
    ARCHITECTURE.md exists with the four core-artifact sections, the shared-store contract section,
    and both production wiring patterns, all reflecting the validated corrections with citations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Mermaid diagrams, the end-to-end data flow, integrated findings, and Phase 2 instrumentation points</name>
  <files>docs/planning/data2/ARCHITECTURE.md</files>
  <action>
    Extend ARCHITECTURE.md with the synthesis content that makes it authoritative end-to-end:

    1. End-to-end data flow narrative: JSON over HTTP -> parsed raw object -> Store mints StoreRecords
       -> central Cube holds leaf facts -> incremental updates arrive via whichever transport (link to
       TRANSPORT-INVENTORY.md and summarize the invariant snapshot-vs-diff ingest contract) -> View
       recomputes ViewResult -> connected Store(s) load -> grid reactions translate to synchronous AG
       Grid transactions -> render. Cross-reference the companion docs rather than duplicating their full
       detail.

    2. Mermaid diagrams (at least two, in ```mermaid fenced blocks):
       - A component/data-flow `graph`/`flowchart` showing raw JSON -> Store/StoreRecord -> Cube
         (internal store) -> View (ViewResult) -> connected Store(s) -> GridModel -> AG Grid, with
         non-grid consumers (charts/toolbars) branching off the shared Store.
       - A `sequenceDiagram` of an incremental update: transport -> cube.updateDataAsync ->
         noteCubeUpdated (imperative) -> View recompute / dataOnlyUpdate -> view.result (observable.ref)
         -> store.loadData -> store._filtered -> Grid dataReaction -> agApi.applyTransaction (sync).
       - (Optional third) a copy-vs-reuse `graph` annotating each transition edge as "ref" vs "copy".
       Keep diagram labels free of private names.

    3. Integrated findings sections - for each Wave 1 deliverable, a concise summary (3-8 bullets)
       plus a link to the full doc:
       - "Copy-vs-reuse (INV-02)" - the per-transition verdicts and the memory-multiplication takeaway,
         linking COPY-VS-REUSE.md.
       - "MobX reaction granularity (INV-03)" - whole-RecordSet/whole-ViewResult observation vs.
         per-record synchronous transaction, mounted-only reactions, linking MOBX-GRANULARITY.md.
       - "Transport inventory (INV-04)" - the five patterns and the invariant ingest contract, linking
         TRANSPORT-INVENTORY.md.

    4. "Phase 2 instrumentation points" section (success criterion #5 - the Phase 2 bridge): name the
       concrete boundaries where heap and timing are attributable and the harness will hook. At minimum:
       - Cube ingest (loadDataAsync / updateDataAsync) - inbound timing + record counts.
       - Cube -> View imperative push (noteCubeUpdated) - re-aggregation compute boundary.
       - View.result assignment (the @observable.ref write) - the MobX seam; result-size attribution.
       - Store.loadData / store._filtered rebuild - grid-store record allocation boundary.
       - Grid dataReaction -> genTransaction -> applyTransaction - main-thread transaction cost +
         the JS<->AG Grid bridge boundary.
       - Heap-attribution layers (cube store records, grid store records, intermediate ViewResults,
         AG Grid internal nodes) flagged from COPY-VS-REUSE.md.
       For each point, note what it measures (heap vs. timing vs. throughput) and tie it to the
       relevant HARN requirement (HARN-03 boundary instrumentation, HARN-04 heap attribution, HARN-05
       compute-vs-bridge split) so Phase 2 can hook directly.

    5. "Open questions carried forward" - consolidate any source-inconclusive items from the three
       companion docs into Phase 2 measurement targets.

    OPEN-REPO RULE: no private client/customer names. Allowed proper names: Hoist, Toolbox, JobSite.
    Plain spaced hyphen " - ", never an em dash.
  </action>
  <verify>
    `grep -c '```mermaid' docs/planning/data2/ARCHITECTURE.md` returns >= 2. Confirm links to all three
    companion docs (`grep -E "COPY-VS-REUSE|MOBX-GRANULARITY|TRANSPORT-INVENTORY" docs/planning/data2/ARCHITECTURE.md`).
    Confirm an instrumentation-points section naming loadDataAsync/updateDataAsync, View.result, and
    applyTransaction (`grep -iE "instrumentation|applyTransaction|View.result" docs/planning/data2/ARCHITECTURE.md`).
    Run the open-repo guard scanner across all docs/planning/data2 files (`--all` if available); zero
    forbidden names.
  </verify>
  <done>
    ARCHITECTURE.md is authoritative end-to-end: >=2 Mermaid diagrams, the full data-flow narrative,
    integrated INV-02/03/04 summaries with links, a Phase 2 instrumentation-points section tied to HARN
    requirements, and carried-forward open questions - all free of forbidden names.
  </done>
</task>

</tasks>

<verification>
- ARCHITECTURE.md covers Store / Cube / View / GridModel and the end-to-end data flow.
- At least two Mermaid diagrams present (component/data-flow + reactivity sequence).
- The doc absorbs/supersedes the validation notes and integrates INV-02/03/04 with links + summaries.
- A Phase 2 instrumentation-points section names concrete boundaries (ingest, View.result, transaction)
  tied to HARN requirements.
- No private client names anywhere in docs/planning/data2.
</verification>

<success_criteria>
INV-01 satisfied and the Phase 2 bridge established: one authoritative, source-grounded architecture
document with Mermaid diagrams supersedes the validation notes, integrates the three deep-dive findings,
and names the instrumentation points the harness will hook.
</success_criteria>

<output>
After completion, create `.planning/phases/01-current-state-inventory/01-04-SUMMARY.md`
</output>
