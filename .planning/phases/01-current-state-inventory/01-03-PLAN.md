---
phase: 01-current-state-inventory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/planning/data2/TRANSPORT-INVENTORY.md
autonomous: true
requirements: [INV-04]
must_haves:
  truths:
    - "A reader can enumerate every update-delivery transport/pattern the layer must support"
    - "For each transport, the doc states how Cube/Store ingest adapts (which API receives the data)"
    - "The distinction between WebSocket data push and WebSocket-as-notification is explicit and grounded"
    - "The full-snapshot vs. partial-diff (isPartial) ingest split is documented (loadDataAsync vs updateDataAsync)"
  artifacts:
    - path: "docs/planning/data2/TRANSPORT-INVENTORY.md"
      provides: "Inventory of transports/patterns and how Cube/Store ingest adapts to each"
      min_lines: 110
      contains: "updateDataAsync"
  key_links:
    - from: "docs/planning/data2/TRANSPORT-INVENTORY.md"
      to: "svc/WebSocketService.ts"
      via: "cited reference to XH.webSocketService subscribe/WebSocketSubscription/WebSocketMessage"
      pattern: "WebSocketService\\.ts|webSocketService"
    - from: "docs/planning/data2/TRANSPORT-INVENTORY.md"
      to: "data/cube/Cube.ts"
      via: "cited reference to loadDataAsync/updateDataAsync ingest entry points"
      pattern: "Cube\\.ts|loadDataAsync|updateDataAsync"
---

<objective>
Produce the transport/pattern inventory (INV-04): an enumeration of the update-delivery transports
and patterns the data layer must support - HTTP snapshot/diff, WebSocket data push via
`XH.webSocketService`, WebSocket-as-notification, SignalR, polling - and a precise account of how
Cube/Store ingest adapts to each (which ingest API receives the data and in what shape).

Purpose: Adaptability across client transports is an overarching project principle. The harness
(Phase 2) must parameterize change-delivery transport, and any Data 2.0 path must stay
transport-agnostic. This inventory defines the surface that must remain pluggable. CRITICAL framing
correction from validation: the local sample apps use HTTP poll-then-diff and WS-as-notification, but
WebSocket data push is a first-class, strategically important pattern - do NOT write it out of the
story or over-anchor to the samples.

Output: docs/planning/data2/TRANSPORT-INVENTORY.md
</objective>

<execution_context>
@/Users/amcclain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amcclain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/planning/data2/KICKOFF-VALIDATION.md
@docs/planning/data2/validation/C-real-usage.md

# Source files this plan must read in detail (do not guess - read them):
@svc/WebSocketService.ts
@data/cube/Cube.ts
@data/Store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map the WebSocket service and the Cube/Store ingest entry points</name>
  <files>docs/planning/data2/TRANSPORT-INVENTORY.md</files>
  <action>
    Read `svc/WebSocketService.ts`, `data/cube/Cube.ts`, and `data/Store.ts`. Use the hoist-react MCP
    tools to confirm the public API (`hoist-search-symbols "WebSocketService"`,
    `hoist-get-members WebSocketService`, `hoist-get-members Cube`, `hoist-search-docs "websocket"`).
    Also confirm the Toolbox portfolio example references named in PROJECT.md / KICKOFF-VALIDATION
    (`PortfolioService` / `PositionSession` using `XH.webSocketService.subscribe`) - Toolbox is a
    public app and MAY be named.

    Document the ingest side first (this is the constant that all transports feed into):
    - `Cube.loadDataAsync()` - full snapshot replacement. Cite Cube.ts.
    - `Cube.updateDataAsync()` - incremental upsert + delete (the `StoreTransaction` shape). Cite
      Cube.ts. Note it delegates to `Store.updateData()` and fans out to connected views.
    - `Store.loadData()` vs `Store.updateData()` - the underlying store-level equivalents. Cite Store.ts.
    - The `isPartial` decision point: server returns either a full snapshot (-> loadDataAsync) or a
      partial diff (-> updateDataAsync) (C-real-usage 2d). Frame this as the universal "snapshot vs.
      diff" fork that every transport ultimately resolves to.

    Document the WebSocket service surface:
    - `XH.webSocketService` with `WebSocketSubscription` / `WebSocketMessage`. List the public
      subscribe/unsubscribe API and the message shape from source. Cite WebSocketService.ts.
  </action>
  <verify>
    Intermediate task - no standalone artifact yet. Confirm via your notes that you have located the
    Cube ingest entry points and the WebSocketService public API with file references. (Combined with
    Task 2 which writes the document.)
  </verify>
  <done>
    Cube/Store ingest entry points (loadDataAsync, updateDataAsync, loadData, updateData) and the
    WebSocketService public API are identified with source references, ready to write up.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the transport inventory mapping each pattern to its ingest adaptation</name>
  <files>docs/planning/data2/TRANSPORT-INVENTORY.md</files>
  <action>
    Using the ingest map and WS API from Task 1, write TRANSPORT-INVENTORY.md. For EACH transport/
    pattern, document: what it is, who controls it (asymmetric control - shapeable for our Grails
    server, fixed for client systems, per PROJECT.md constraints), and exactly how the data reaches
    the Cube/Store ingest entry points.

    Patterns to cover (at minimum the five named in INV-04):
    1. HTTP snapshot/diff (poll-then-diff) - server returns full snapshot OR `isPartial` diff;
       client routes to `cube.loadDataAsync` vs `cube.updateDataAsync`. This is the dominant pattern
       in the local sample apps (C-real-usage 2d) - cite the pattern generically, NOT the app names.
    2. WebSocket data push via `XH.webSocketService` - subscription delivers data payloads that feed
       `updateDataAsync`. First-class Hoist capability; demonstrated in Toolbox's portfolio example
       (PortfolioService / PositionSession). Emphasize this is strategically important and NOT
       represented in the local sample apps - do not under-weight it.
    3. WebSocket-as-notification - a lightweight "refresh ready" message triggers an HTTP fetch; the
       HTTP response (not the WS payload) flows into the cube. Document the message-carries-metadata
       shape (e.g. progress/success flags), generically (C-real-usage 2e/3).
    4. SignalR - named as a real client transport in the project framing; document it as another
       push/notify channel that ultimately resolves to the same snapshot-vs-diff ingest fork. Note if
       there is no Hoist-native SignalR client (it is a client-system transport Hoist must adapt to).
    5. Polling - periodic HTTP fetch (degenerate case of #1 without server-side diffing, or with it).

    Add a synthesis section: "The invariant ingest contract." Make the key architectural point that
    regardless of transport, ingest collapses to two operations - full snapshot (loadDataAsync) or
    incremental diff (updateDataAsync) - so transport-agnosticism is achievable because the layer
    only needs an adapter that lands data at these two entry points. This is the contract the harness
    must parameterize (Phase 2 HARN-02) and that any Data 2.0 path must preserve.

    Add a short table: Transport | Control (ours/fixed) | Reaches ingest via | Snapshot or diff | Notes.

    Reference the adaptability principle from PROJECT.md: transport-specific optimizations are valuable
    but must be labeled conditional, not the default path.

    OPEN-REPO RULE: no private client/customer names. Allowed proper names: Hoist, Toolbox, JobSite.
    Describe sample-app patterns generically ("a client app uses WS-as-notification"). Use a plain
    spaced hyphen " - ", never an em dash, in all prose.
  </action>
  <verify>
    `test -f docs/planning/data2/TRANSPORT-INVENTORY.md`. Confirm all five patterns appear
    (`grep -iE "poll-then-diff|websocket|signalr|polling|notification" docs/planning/data2/TRANSPORT-INVENTORY.md`).
    Confirm both `loadDataAsync` and `updateDataAsync` are referenced. Run the open-repo guard scanner
    against the new file; zero forbidden names.
  </verify>
  <done>
    TRANSPORT-INVENTORY.md exists, enumerates all five transports/patterns, states for each how
    Cube/Store ingest adapts (snapshot vs diff via loadDataAsync/updateDataAsync), distinguishes WS
    push from WS-notification, captures the invariant ingest contract, and contains no forbidden names.
  </done>
</task>

</tasks>

<verification>
- All five named transports/patterns are inventoried.
- Each maps to a concrete Cube/Store ingest path (loadDataAsync vs updateDataAsync).
- WS data push vs WS-as-notification is explicitly distinguished and correctly attributed.
- The invariant snapshot-vs-diff ingest contract is articulated as the basis for transport-agnosticism.
- No private client names.
</verification>

<success_criteria>
INV-04 satisfied: the transports/patterns the layer must support are inventoried, with how Cube/Store
ingest adapts to each, framed so that WebSocket push is not under-weighted relative to the local samples.
</success_criteria>

<output>
After completion, create `.planning/phases/01-current-state-inventory/01-03-SUMMARY.md`
</output>
