---
phase: 03-baseline-performance-envelope
plan: 06
type: execute
wave: 4
depends_on: ["03-01", "03-04", "03-05"]
files_modified:
  - docs/planning/data2/TARGETS-PROPOSAL.md
  - data/measure/types.ts
  - docs/planning/data2/BASELINE.md
autonomous: false
requirements: [BASE-04]
must_haves:
  truths:
    - "Each proposed target carries its measurement evidence and rationale (hybrid floor + aspiration)"
    - "Both envelope-level targets (max-shape, sustained-throughput) are proposed with ladder-boundary evidence, alongside the four per-scorecard targets"
    - "The user approves or adjusts each number at a human-verify checkpoint BEFORE it is committed"
    - "The adopted numbers populate DEFAULT_TARGETS and are finalized in BASELINE.md as the source of truth"
    - "The two envelope-level targets have a documented ladder-evaluated pass/fail derivation (evaluateEnvelope over EnvelopeSummary) in BASELINE.md, not per-scorecard badges"
  artifacts:
    - path: "docs/planning/data2/TARGETS-PROPOSAL.md"
      provides: "Each metric: floor + aspiration + evidence + rationale for review"
      contains: "floor"
    - path: "data/measure/types.ts"
      provides: "DEFAULT_TARGETS populated with the adopted numbers (no longer provisional)"
      contains: "DEFAULT_TARGETS"
    - path: "docs/planning/data2/BASELINE.md"
      provides: "Adopted-targets section + source-of-truth pointer to TargetsConfig + envelope-level derivation"
      contains: "## Adopted Targets"
  key_links:
    - from: "docs/planning/data2/BASELINE.md"
      to: "data/measure/types.ts DEFAULT_TARGETS"
      via: "report names TargetsConfig/DEFAULT_TARGETS as the source of truth"
      pattern: "DEFAULT_TARGETS"
---

<objective>
Convert the measured envelope (coarse + zoom + small-heap) into defensible quantitative targets, get
them adopted at a human-verify checkpoint (D-08), and wire the approved numbers into the framework
source of truth. Two-tier per metric (D-07): a "must-hold" floor derived from the measured baseline (no
Phase 6+ candidate may regress it) and an aspirational target derived from business need (~500x20
sub-second at trading-screen cadence; bounded per-tab memory on reference + small-heap machines).
Covers ALL four BASE-04 target families - the four per-scorecard metrics AND the two envelope-level
metrics (max records x fields; sustained batch size/rate without jank). Numbers are NOT committed to
`DEFAULT_TARGETS` until the user approves them.

Purpose: BASE-04 - targets proposed, adopted, and wired as harness pass/fail criteria for all later
candidate evaluation. This is the gate that turns the baseline into the ruler Phase 6 uses.
Output: TARGETS-PROPOSAL.md, populated DEFAULT_TARGETS, finalized BASELINE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@docs/planning/data2/BASELINE.md
@docs/planning/data2/stats/envelope-stats.json
@data/measure/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the targets proposal with evidence and rationale</name>
  <files>docs/planning/data2/TARGETS-PROPOSAL.md</files>
  <read_first>
    - docs/planning/data2/stats/envelope-stats.json (coarse series + tier boundaries + anchor breakdown)
    - docs/planning/data2/stats/README.md (small-heap + zoom runs, named machines)
    - docs/planning/data2/BASELINE.md (envelope narrative feeding the numbers)
    - data/measure/types.ts (the exact TargetsConfig metric fields + EnvelopeSummary the proposal must fill)
    - .planning/phases/03-baseline-performance-envelope/03-CONTEXT.md (D-07 hybrid floor+aspiration; business anchor)
  </read_first>
  <action>
    Create `docs/planning/data2/TARGETS-PROPOSAL.md`. For EACH TargetsConfig metric (from types.ts, ALL
    REQUIRED - none dropped to "etc."): the four PER-SCORECARD metrics updateRenderLatencyMs, enginePcpuMs,
    heapCeilingReferenceBytes, heapCeilingSmallHeapBytes, AND the two ENVELOPE-LEVEL metrics maxRecordsXFields
    (max leaf-records x fields sustained in the comfortable tier - BASE-04 "max records x fields client-side")
    and sustainedThroughput (max batchSize x ratePerSec without breaching the jank wall). For each present:
    the proposed floor (baseline-derived regression guard) and aspiration (business-need goal) with units;
    the MEASUREMENT EVIDENCE (which runs/rungs from the stats dir + the observed numbers) supporting each; and
    the RATIONALE (frame-budget ~100 ms for engine+grid-sync p95 per D-03; keep-up threshold vs batch interval;
    per-tab memory ceilings reference vs small-heap machine; ~500x20 sub-second update->render latency). For the
    two envelope-level metrics the evidence is the ladder boundary - the max comfortable records x fields and the
    max keep-up throughput read off the coarse/zoom ladder (the EnvelopeSummary) - and the proposal MUST state
    that their pass/fail is derived via evaluateEnvelope and documented in BASELINE.md, NOT rendered as
    per-scorecard badges. Frame the tiered memory boundaries (green/yellow/red, D-02) with their evidence.
    Describe the business trading-cadence anchor GENERICALLY (open-repo: never by client name). Present as a
    review-ready table plus per-metric notes so the user can approve or adjust each number at the Task 2
    checkpoint. No em dashes (use spaced hyphen).
  </action>
  <verify>
    <automated>test -f /Users/amcclain/dev/hoist-react/docs/planning/data2/TARGETS-PROPOSAL.md && grep -q floor /Users/amcclain/dev/hoist-react/docs/planning/data2/TARGETS-PROPOSAL.md && grep -q aspiration /Users/amcclain/dev/hoist-react/docs/planning/data2/TARGETS-PROPOSAL.md && grep -q maxRecordsXFields /Users/amcclain/dev/hoist-react/docs/planning/data2/TARGETS-PROPOSAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/planning/data2/TARGETS-PROPOSAL.md` exists and lists every TargetsConfig metric with a floor + aspiration + unit
    - Both envelope-level metrics (maxRecordsXFields, sustainedThroughput) appear with floor + aspiration + ladder-boundary evidence
    - The proposal states the two envelope-level metrics are evaluated via evaluateEnvelope + documented in BASELINE.md (not per-scorecard badges)
    - Each metric row cites its measurement evidence (specific runs/rungs) and a rationale
    - The business trading-cadence anchor is described generically (no private client name)
    - No em dash appears in the file
  </acceptance_criteria>
  <done>A review-ready proposal presents each target's floor/aspiration (all four BASE-04 families) with evidence and rationale.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Target adoption checkpoint (D-08)</name>
  <files>(no source files - approval gate over TARGETS-PROPOSAL.md)</files>
  <read_first>
    - docs/planning/data2/TARGETS-PROPOSAL.md (the numbers under review)
    - .planning/phases/03-baseline-performance-envelope/03-CONTEXT.md (D-08: targets are not adopted until this checkpoint passes)
  </read_first>
  <action>
    Present the full TARGETS-PROPOSAL.md - each metric's floor + aspiration + evidence + rationale - for the
    user to approve or adjust; record the final adopted numbers for Task 3. No number is committed until this
    gate passes (D-08).
  </action>
  <what-built>
    The full TARGETS-PROPOSAL.md - each metric's floor + aspiration + evidence + rationale, including the two
    envelope-level metrics - is presented for the user to approve or adjust BEFORE any number is committed into
    DEFAULT_TARGETS or the report. This is the D-08 adoption gate: no target is "adopted" until the user signs off.
  </what-built>
  <how-to-verify>
    1. Review each proposed floor and aspiration against its cited evidence in TARGETS-PROPOSAL.md.
    2. Approve each number, or provide adjusted values.
    3. Confirm the small-heap heap ceiling reflects the named small-heap machine from 03-05.
    4. Confirm the two envelope-level targets (maxRecordsXFields, sustainedThroughput) match the observed ladder boundary.
  </how-to-verify>
  <verify>
    <human-check>User has approved or adjusted every target number (all four BASE-04 families); the final adopted set is recorded for Task 3</human-check>
  </verify>
  <acceptance_criteria>
    - Every metric has an explicit adopted floor + aspiration recorded (approved or adjusted)
    - Both envelope-level targets are among the recorded adopted set
    - The adopted set is captured verbatim so Task 3 can transcribe it into DEFAULT_TARGETS
  </acceptance_criteria>
  <resume-signal>Type "adopted" with any adjusted numbers, or list changes to make before adoption.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit adopted numbers into DEFAULT_TARGETS and finalize BASELINE.md</name>
  <files>data/measure/types.ts, docs/planning/data2/BASELINE.md</files>
  <read_first>
    - data/measure/types.ts (the provisional DEFAULT_TARGETS + TargetsConfig + EnvelopeSummary shape from 03-01)
    - docs/planning/data2/TARGETS-PROPOSAL.md (the approved numbers)
    - docs/planning/data2/BASELINE.md (coarse draft to finalize)
  </read_first>
  <action>
    Populate `DEFAULT_TARGETS` in data/measure/types.ts with the ADOPTED numbers from the Task 2 checkpoint
    (ALL four families incl. maxRecordsXFields + sustainedThroughput, with `higherIsBetter: true` on the two
    envelope-level metrics), replacing the provisional placeholder and removing the "provisional" JSDoc note
    (replace it with a note citing the adoption date + that BASELINE.md is the derivation record). Then finalize
    docs/planning/data2/BASELINE.md: remove the "coarse pass" status banner, fold in the zoom + small-heap
    findings, and add an `## Adopted Targets` section that names `TargetsConfig`/`DEFAULT_TARGETS` in
    data/measure as the source of truth and lists the adopted floor/aspiration per metric. For the two
    ENVELOPE-LEVEL metrics (maxRecordsXFields, sustainedThroughput), document their pass/fail DERIVATION
    explicitly: the ladder boundary facts (EnvelopeSummary) evaluated via `evaluateEnvelope` against the
    adopted floors/aspirations, and state they are report-documented + ladder-evaluated, NOT per-scorecard
    badges. Record the observed EnvelopeSummary values (max comfortable records x fields; max keep-up
    throughput) so the verdict is reproducible. If populating DEFAULT_TARGETS makes it a user-facing
    hoist-react change, read docs/changelog-format.md and add a CHANGELOG.md entry under the topmost -SNAPSHOT
    heading. No em dashes; no private client names.
  </action>
  <verify>
    <automated>cd /Users/amcclain/dev/hoist-react && npx tsc --noEmit && yarn lint:code && grep -q "## Adopted Targets" docs/planning/data2/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `DEFAULT_TARGETS` in data/measure/types.ts holds the adopted numbers for all four BASE-04 families (no provisional-sentinel values, no "provisional" note)
    - `npx tsc --noEmit` exits 0 and `yarn lint:code` exits 0
    - `docs/planning/data2/BASELINE.md` contains `## Adopted Targets` naming DEFAULT_TARGETS as the source of truth
    - The `## Adopted Targets` section documents the envelope-level (maxRecordsXFields, sustainedThroughput) pass/fail derivation via evaluateEnvelope over the ladder EnvelopeSummary, and records the observed boundary values
    - BASELINE.md no longer carries the "coarse pass" status banner
    - No em dash and no forbidden client name in the edited files
  </acceptance_criteria>
  <done>The adopted targets (all four BASE-04 families) are the framework source of truth (DEFAULT_TARGETS) and the envelope report is finalized with them, including the envelope-level derivation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit -> public repo | Proposal/report could leak private client names (open-repo rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | TARGETS-PROPOSAL.md + BASELINE.md | mitigate | Open-repo rule: trading-cadence anchor described generically; only Hoist/Toolbox/JobSite named; PreToolUse guard on commit |
| T-03-06b | Integrity (premature adoption) | DEFAULT_TARGETS numbers | mitigate | Numbers committed ONLY after the D-08 human-verify checkpoint (Task 2); provisional until then (RESEARCH anti-pattern) |

No package installs; no legitimacy gate.
</threat_model>

<verification>
- `npx tsc --noEmit` + `yarn lint:code` clean after DEFAULT_TARGETS populated
- TARGETS-PROPOSAL.md lists floor+aspiration+evidence per metric incl. both envelope-level metrics
- BASELINE.md has `## Adopted Targets` naming DEFAULT_TARGETS + envelope-level derivation; no em dashes/client names
</verification>

<success_criteria>
Defensible targets (all four BASE-04 families) are proposed with evidence, adopted at the D-08 checkpoint,
and wired into DEFAULT_TARGETS + BASELINE.md as the pass/fail source of truth for Phase 6+ (BASE-04), with
the two envelope-level targets given an explicit ladder-evaluated derivation path.
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-06-SUMMARY.md` when done.
</output>
</content>
