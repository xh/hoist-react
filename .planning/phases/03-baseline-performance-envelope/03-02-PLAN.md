---
phase: 03-baseline-performance-envelope
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../toolbox/client-app/src/examples/datalab/DataLabModel.ts
  - ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
must_haves:
  truths:
    - "A user can download a single saved run, or all saved runs, as JSON from the Data Lab UI"
    - "A user can import a run JSON file, which is shape-validated before entering savedRuns"
    - "A user can export a distilled, chart-ready envelope-stats JSON for design-tool consumption"
  artifacts:
    - path: "../toolbox/client-app/src/examples/datalab/DataLabModel.ts"
      provides: "exportRun / exportAllRuns / importRunsAsync / exportDistilledStats methods"
      contains: "downloadBlob"
    - path: "../toolbox/client-app/src/examples/datalab/DataLabPanel.ts"
      provides: "Export / Export All / Import / Export Distilled controls"
      contains: "fileChooser"
  key_links:
    - from: "DataLabModel.importRunsAsync"
      to: "savedRuns"
      via: "shape-validated parse then set new savedRuns array (@persist writes through)"
      pattern: "this.savedRuns = "
    - from: "DataLabModel export methods"
      to: "@xh/hoist/utils/js downloadBlob"
      via: "Blob(JSON) -> downloadBlob"
      pattern: "downloadBlob\\("
---

<objective>
Add the run capture + stats package plumbing to the Toolbox Data Lab app (D-11/D-12): export a single
run or all saved runs as JSON, import a run JSON file (shape-validated against RunResult before it is
trusted), and export a distilled, flat, chart-ready envelope-stats JSON shaped for direct design-tool
consumption. This is the capture path that turns localStorage-local runs into files in the checked-in
stats directory and enables cross-machine (small-heap) side-by-side comparison.

Purpose: The presentation deliverable (front-loaded) and the small-heap cross-machine comparison both
depend on getting runs out as files. This plan builds that path so the coarse sweep (03-03) can export.
Output: export/import + distilled-stats controls + methods in DataLabModel.ts / DataLabPanel.ts.

Uses only EXISTING @xh/hoist/data exports (RunResult, ScenarioConfig, Scorecard) - it does NOT depend
on plan 03-01's new exports, so it can run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@.planning/phases/03-baseline-performance-envelope/03-PATTERNS.md
@data/measure/types.ts
@utils/js/DownloadUtils.ts

<interfaces>
Existing Data Lab seams to extend (from DataLabModel.ts):
- `interface SavedRun { label: string; savedAt: string; result: RunResult; }` (line 24)
- `@persist.with({localStorageKey: 'dataLab.savedRuns'}) savedRuns: SavedRun[]` (line 125-126) - set a NEW array to persist
- `private recordRun(result: RunResult)` (line 436) - append pattern to mirror on import
- `async clearSavedRunsAsync()` (line 443) - confirm-then-mutate async pattern
- `get comparisonRows(): PlainObject[]` (line 470)
- `fmtMs` / `fmtBytes` helpers (line 553/557)
Framework utilities:
- `downloadBlob(blob: Blob, filename: string)` from `@xh/hoist/utils/js`
- `fileChooser` + `FileChooserModel` from `@xh/hoist/desktop/cmp/filechooser`
- `Icon.*`, `button`, `toolbar`, `toolbarSep`, `filler` (already imported in DataLabPanel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run export (single + all) and distilled-stats export in DataLabModel + controls in DataLabPanel</name>
  <files>../toolbox/client-app/src/examples/datalab/DataLabModel.ts, ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts</files>
  <read_first>
    - ../toolbox/client-app/src/examples/datalab/DataLabModel.ts (full - recordRun, savedRuns, comparisonRows, fmtBytes/fmtMs, import block)
    - ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts (scorecard, comparisonPanel tbar, run-controls bbar button patterns)
    - utils/js/DownloadUtils.ts (downloadBlob signature - do NOT hand-roll a Blob/anchor dance)
    - data/measure/types.ts (RunResult, Scorecard, EnvMetadata shapes the distilled stats read)
  </read_first>
  <action>
    In DataLabModel.ts add `import {downloadBlob} from '@xh/hoist/utils/js';` and:
    - `exportRun(run: SavedRun)`: `downloadBlob(new Blob([JSON.stringify(run.result, null, 2)], {type: 'application/json'}), 'datalab-run-<sanitized-label>.json')`.
    - `exportAllRuns()`: same but payload is the full `savedRuns` array (or `savedRuns.map(r => r.result)`); filename `datalab-runs-all-<timestamp>.json`.
    - `exportDistilledStats()`: build a FLAT, chart-ready object and downloadBlob it as `datalab-envelope-stats.json`. Schema (D-12, Claude's discretion) - a single object with:
      `{ generatedAt, env: EnvMetadata[], memorySeries: [{leafRowCount, fieldCount, totalHeapDeltaBytes, cubeStoreRecords, gridStoreRecords, viewResultRows, agGridInternals}], cpuSeries: [{batchSize, ratePerSec, cadence, engineMedianMs, engineP95Ms, genTxnMedianMs, bridgeMedianMs, renderMedianMs, endToEndMedianMs}], anchorBatch: {batchSize:500, fieldCount:20, stageBreakdownMs:{engine,genTxn,bridge,render,endToEnd}} }`.
      Derive each series row from `savedRuns` by reading `result.scenario.dataset`/`result.scenario.update`/`result.scorecard`. Use lodash for grouping/mapping. The goal is the design tool consumes this WITHOUT parsing verbose RunResult objects.
    In DataLabPanel.ts add controls to the comparisonPanel tbar (or a new export toolbar): `button({text: 'Export All', icon: Icon.download(), onClick: () => model.exportAllRuns()})`, `button({text: 'Export Stats', icon: Icon.fileArchive() or Icon.download(), onClick: () => model.exportDistilledStats()})`, and a per-run Export button where individual runs are listed. Use element factories + `Icon.*`, never raw HTML.
    CRITICAL: `Async` suffix only on promise-returning methods (these are sync); `null` over `undefined`; no em dashes in comments.
  </action>
  <verify>
    <automated>cd /Users/amcclain/dev/toolbox && yarn lint:types && yarn lint:code</automated>
  </verify>
  <acceptance_criteria>
    - DataLabModel.ts contains `downloadBlob(` and methods `exportRun`, `exportAllRuns`, `exportDistilledStats`
    - DataLabModel.ts imports `downloadBlob` from `@xh/hoist/utils/js`
    - DataLabPanel.ts contains Export All + Export Stats buttons wired to model methods
    - Toolbox `yarn lint:types` (tsc) exits 0 and `yarn lint:code` exits 0
    - No raw HTML button/anchor added (grep shows no `<button` / `document.createElement`)
  </acceptance_criteria>
  <done>Export of single run, all runs, and distilled chart-ready stats works via framework downloadBlob; controls render in the Data Lab UI.</done>
</task>

<task type="auto">
  <name>Task 2: Validated run import via FileChooser</name>
  <files>../toolbox/client-app/src/examples/datalab/DataLabModel.ts, ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts</files>
  <read_first>
    - ../toolbox/client-app/src/examples/datalab/DataLabModel.ts (recordRun append pattern, clearSavedRunsAsync confirm pattern, savedRuns @persist)
    - desktop/cmp/filechooser/FileChooserModel.ts and FileChooser.ts (the sanctioned Hoist file-select surface + its selected-file API)
    - desktop/tabs/other/FileChooserPanel.ts in Toolbox (reference usage of fileChooser)
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Security Domain V5 + Pitfall 6: validate untrusted JSON, preserve EnvMetadata)
  </read_first>
  <action>
    Add `importRunsAsync(text: string)` (or accept the selected file) to DataLabModel.ts. It must:
    1. `JSON.parse` the text inside a try/catch (never `eval`).
    2. Accept either a single RunResult object or an array of RunResult (from Export All).
    3. SHAPE-VALIDATE each parsed object before trusting it: assert `result.scorecard` is an object with the
       expected numeric-or-null stage fields (`engine`, `genTxn`, `bridgeCall`, `render` each `TimingStat | null`),
       `result.scorecard.rowCounts` present with numeric `leaf`/`aggregate`/`gridRows`, `result.env` present
       (preserve EnvMetadata so the origin machine is never lost - Pitfall 6), and `result.scenario` present.
       Reject (toast an error via `XH.toast`/`XH.dangerAlert`, do NOT add to savedRuns) any object that fails,
       so a malformed file cannot crash the comparison grid.
    4. Build `SavedRun` records for valid entries (label from `result.scenario.name` + `result.env.capturedAt`,
       mirroring `recordRun`) and set a NEW `savedRuns` array (append) so `@persist` writes through.
    In DataLabPanel.ts wire a `fileChooser` (from `@xh/hoist/desktop/cmp/filechooser`) with an accompanying
    Import button that reads the chosen file's text and calls `model.importRunsAsync(text)`. Use Hoist
    components, not a raw `<input type=file>`.
    CRITICAL: `Async` suffix on the promise method; `@action` on the observable mutation; `null` over `undefined`
    (check `== null`); no em dashes.
  </action>
  <verify>
    <automated>cd /Users/amcclain/dev/toolbox && yarn lint:types && yarn lint:code</automated>
  </verify>
  <acceptance_criteria>
    - DataLabModel.ts contains `importRunsAsync` using `JSON.parse` inside try/catch and NO `eval`
    - Import path shape-validates `scorecard.rowCounts`, stage timing fields, and `env` before adding to savedRuns
    - Invalid input is rejected with a user-facing toast/alert and does not enter savedRuns
    - DataLabPanel.ts uses `fileChooser` from `@xh/hoist/desktop/cmp/filechooser` (grep shows the import) + an Import control
    - Toolbox `yarn lint:types` exits 0 and `yarn lint:code` exits 0
  </acceptance_criteria>
  <done>A valid run JSON imports into savedRuns and appears in comparison; a malformed file is rejected without crashing the grid; EnvMetadata is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file -> app | Imported run JSON (D-11) is untrusted input parsed into app state and rendered in the comparison grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering / DoS | DataLabModel.importRunsAsync | mitigate | JSON.parse in try/catch (never eval); explicit RunResult/Scorecard shape validation (numeric stage fields, rowCounts, env) before adding to savedRuns; reject malformed input with a toast so a bad file cannot crash the comparison grid |
| T-03-02b | Information disclosure | exported JSON / distilled stats files | accept | Exported files are dev-local; when checked into the stats dir (03-03/03-04) the open-repo no-client-names rule + PreToolUse guard apply. EnvMetadata carries only userAgent/flags, no user data |

No package installs in this plan; no legitimacy gate required.
</threat_model>

<verification>
- Toolbox `yarn lint:types` (tsc) + `yarn lint:code` clean
- Import path rejects a hand-crafted malformed JSON without adding to savedRuns (manual live spot-check)
- Export produces a downloadable JSON; distilled stats is flat/chart-ready
</verification>

<success_criteria>
Runs export (single/all) and distilled envelope-stats export as JSON via framework downloadBlob; run JSON
imports through a shape-validated guard that preserves EnvMetadata and rejects malformed input.
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-02-SUMMARY.md` when done.
</output>
