---
phase: 03-baseline-performance-envelope
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - docs/planning/data2/BASELINE.md
  - docs/planning/data2/stats/envelope-stats.json
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
must_haves:
  truths:
    - "A distilled, chart-ready envelope-stats JSON derived from the coarse pass exists in the stats dir"
    - "A coarse-draft BASELINE.md narrates the current stack, where the walls are, and how memory/CPU/latency scale"
    - "The package is design-tool-ready and does not wait for zoom or target adoption"
  artifacts:
    - path: "docs/planning/data2/stats/envelope-stats.json"
      provides: "Flat scaling series + tier-boundary points + ~500x20 stage breakdown + env metadata"
      contains: "memorySeries"
    - path: "docs/planning/data2/BASELINE.md"
      provides: "Canonical envelope report (coarse draft): stack outline, walls, scaling curves, methodology"
      contains: "## Methodology"
  key_links:
    - from: "docs/planning/data2/BASELINE.md"
      to: "docs/planning/data2/stats/envelope-stats.json"
      via: "narrative references the distilled series + tier boundaries"
      pattern: "envelope-stats"
---

<objective>
Ship the PRIORITY presentation deliverable from the coarse pass (user directive - do NOT gate on zoom
or target adoption): a distilled, flat, chart-ready `envelope-stats.json` and a coarse-draft
`BASELINE.md`. Together these are the package handed to the Claude design tool to build an
infographic-heavy walkthrough of the current stack, where its walls are, and how performance/memory
scale across the D-04 axes. Audience: technical devs first (stage breakdowns, scaling curves,
methodology) plus IT decision-makers (per-tab memory ceilings, machine specs, headroom).

Purpose: The forcing function of the phase - the presentation must land as soon as a reasonable coarse
exploration exists. Targets are proposed later (03-06); the tiered green/yellow/red framing here is
descriptive of observed data, not adopted pass/fail.
Output: `docs/planning/data2/stats/envelope-stats.json` + `docs/planning/data2/BASELINE.md` (coarse draft).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@docs/planning/data2/ARCHITECTURE.md
@data/measure/README.md
@data/measure/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distill the coarse runs into a flat chart-ready envelope-stats.json</name>
  <files>docs/planning/data2/stats/envelope-stats.json</files>
  <read_first>
    - docs/planning/data2/stats/README.md (the manifest + raw run JSON captured in 03-03)
    - docs/planning/data2/stats/*.json (the raw coarse runs to distill)
    - data/measure/types.ts (RunResult / Scorecard / HeapAttribution / EnvMetadata field names)
  </read_first>
  <action>
    Produce `docs/planning/data2/stats/envelope-stats.json` - a single FLAT object the design tool consumes
    without parsing verbose RunResults. If the 03-02 "Export Stats" control was used, save its output here;
    otherwise construct it from the raw stats JSON. Schema (D-12, Claude's discretion):
    - `generatedAt` (ISO-8601), `referenceMachine` (name/spec string from the 03-03 manifest), `env` (array of the distinct EnvMetadata used)
    - `memorySeries`: one row per memory rung with `{leafRowCount, fieldCount, totalHeapDeltaBytes, cubeStoreRecords, gridStoreRecords, viewResultRows, agGridInternals, tier}` where `tier` is the descriptive comfortable/degraded/hardWall label Claude assigns from the observed data (D-02, proposed with evidence)
    - `cpuSeries`: one row per CPU rung with `{batchSize, ratePerSec, cadence, engineMedianMs, engineP95Ms, genTxnMedianMs, bridgeMedianMs, renderMedianMs, endToEndMedianMs, batchIntervalMs, keepsUp}` where `batchIntervalMs = 1000/ratePerSec`, `endToEndMedianMs` = sum of the four stage medians, and `keepsUp = endToEndMedianMs <= batchIntervalMs` (D-03 keep-up condition)
    - `anchorBatch`: the ~500x20 stage breakdown `{batchSize, fieldCount, engineMs, genTxnMs, bridgeMs, renderMs, endToEndMs}` (BASE-03)
    - `tierBoundaries`: the approximate memory/CPU boundary points Claude reads off the coarse ladder (green->yellow and yellow->red), each with the shape/cadence at which it was observed and a one-line rationale
    OPEN-REPO: no private client names anywhere. No em dashes in any string values.
  </action>
  <verify>
    <automated>node -e "const s=require('/Users/amcclain/dev/hoist-react/docs/planning/data2/stats/envelope-stats.json'); if(!s.memorySeries||!s.cpuSeries||!s.anchorBatch) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/planning/data2/stats/envelope-stats.json` parses as JSON and has `memorySeries`, `cpuSeries`, `anchorBatch`, `tierBoundaries`
    - Each `cpuSeries` row carries `batchIntervalMs` and a boolean `keepsUp` (D-03)
    - `anchorBatch` has the 4-stage breakdown + `endToEndMs`
    - `referenceMachine` is populated from the 03-03 manifest
    - No forbidden private client name and no em dash appears in the file
  </acceptance_criteria>
  <done>A single flat envelope-stats.json holds the scaling series, tier boundaries, and anchor breakdown - ready for the design tool.</done>
</task>

<task type="auto">
  <name>Task 2: Write the coarse-draft BASELINE.md narrative</name>
  <files>docs/planning/data2/BASELINE.md</files>
  <read_first>
    - docs/planning/data2/ARCHITECTURE.md (current-stack outline source material for the presentation)
    - docs/planning/data2/stats/envelope-stats.json (the distilled series this narrative references)
    - data/measure/README.md (methodology: forced-GC protocol, heap attribution, stage split, Chrome flags)
    - .planning/phases/03-baseline-performance-envelope/03-CONTEXT.md (dual-audience framing, D-02 green/yellow/red story)
  </read_first>
  <action>
    Create `docs/planning/data2/BASELINE.md` as the canonical envelope report (coarse draft - a "## Status:
    coarse pass; zoom + adopted targets pending" banner at top). Structure (Claude's discretion) covering:
    - `## Current Stack` - concise outline of Cube/View/Store/GridModel and the update->render pipeline, sourced from ARCHITECTURE.md, aimed at the presentation's stack-outline slide.
    - `## Methodology` - the harness, forced-GC/median-p95 protocol, heap-by-layer attribution, the 4-stage split, required Chrome flags, foregrounded-tab note, and the NAMED reference machine.
    - `## Memory Envelope` - scaling narrative across leafRowCount x fieldCount with the green/yellow/red tiered story (D-02); reference the memorySeries + tierBoundaries.
    - `## CPU / Main-Thread Envelope` - steady vs burst; the keep-up condition (median update->render vs batch interval) and the frame-budget condition (p95 engine+grid-sync ~100 ms); reference cpuSeries.
    - `## Update->Render Latency (~500x20)` - the anchor batch stage breakdown (BASE-03).
    - `## For IT Decision-Makers` - per-tab memory ceilings, machine specs, scaling headroom in plain terms.
    - `## Open Items` - zoom refinement + small-heap machine pass + target adoption still pending (forward pointer to 03-05/03-06).
    OPEN-REPO: describe the trading-cadence business anchor generically (e.g. "a real-time trading-screen
    workload"), NEVER by client name. No em dashes (use ` - `). Allowed names: Hoist, Toolbox, JobSite.
  </action>
  <verify>
    <automated>test -f /Users/amcclain/dev/hoist-react/docs/planning/data2/BASELINE.md && grep -q "## Methodology" /Users/amcclain/dev/hoist-react/docs/planning/data2/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/planning/data2/BASELINE.md` exists with sections: Current Stack, Methodology, Memory Envelope, CPU / Main-Thread Envelope, Update->Render Latency, For IT Decision-Makers, Open Items
    - The named reference machine appears in the Methodology section (BASE-01)
    - The report references `envelope-stats.json`
    - `grep -n "—" docs/planning/data2/BASELINE.md` returns nothing (no em dashes)
    - No forbidden private client name appears (open-repo guard passes)
  </acceptance_criteria>
  <done>A coarse-draft BASELINE.md + envelope-stats.json form a shippable, design-tool-ready presentation package from the coarse pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit -> public repo | Report + stats could leak private client names (open-repo rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information disclosure | BASELINE.md + envelope-stats.json | mitigate | Open-repo rule enforced: trading-cadence anchor described generically, only Hoist/Toolbox/JobSite named; PreToolUse guard blocks forbidden names on commit |

No package installs; no legitimacy gate.
</threat_model>

<verification>
- envelope-stats.json parses and has the required top-level keys
- BASELINE.md has all sections + names the reference machine, references the stats file
- No em dashes, no forbidden client names
</verification>

<success_criteria>
The front-loaded presentation package (distilled stats + coarse BASELINE.md) ships from the coarse pass,
design-tool-ready, before zoom and target adoption.
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-04-SUMMARY.md` when done.
</output>
