---
phase: 03-baseline-performance-envelope
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - .mcp.json
  - .claude/settings.local.json
  - docs/planning/data2/stats/README.md
autonomous: false
requirements: [BASE-01, BASE-02, BASE-03]
user_setup:
  - service: chrome-devtools-mcp
    why: "Performance traces (long tasks, dropped frames) corroborating the D-03 jank wall; Claude-driven runs in a foregrounded window"
    dashboard_config:
      - task: "Register chrome-devtools-mcp in .mcp.json and enable in .claude/settings.local.json"
        location: "project .mcp.json + local settings"
  - service: toolbox-dev-servers
    why: "Grails test-data API (:8080) + webpack dev server (:3000) must both be up to serve the Data Lab and drive sweep transports"
    dashboard_config:
      - task: "Launch Grails API and startWithHoist webpack server; log into the browser"
        location: "Toolbox local checkout"
must_haves:
  truths:
    - "chrome-devtools-mcp is installed, version-pinned, and human-verified as legitimate before use"
    - "A named, reproducible ladder of memory rungs and CPU rungs is saved as scenario ViewManager profiles"
    - "A coarse ladder pass has been run on the reference machine and raw run JSON exported into the stats dir"
  artifacts:
    - path: ".mcp.json"
      provides: "chrome-devtools-mcp server registration (pinned version)"
      contains: "chrome-devtools"
    - path: "docs/planning/data2/stats/README.md"
      provides: "Stats dir manifest describing the checked-in raw run JSON + env/machine per run"
      contains: "reference machine"
  key_links:
    - from: "coarse sweep runs"
      to: "docs/planning/data2/stats/"
      via: "Data Lab export (03-02) -> checked-in raw run JSON files"
      pattern: "datalab-run"
---

<objective>
Stand up the sweep environment and produce the FIRST coarse ladder pass whose results feed the
front-loaded presentation package (03-04). Three prerequisites then the coarse run: (1) install +
human-verify chrome-devtools-mcp and confirm the target Toolbox branch (RESEARCH Open Q1); (2) launch
the Grails API + webpack dev server in a foregrounded flagged Chrome and define/save the named ladder
scenario profiles (D-01/D-04); (3) run the COARSE ladder (memory + CPU, reference machine), export raw
run JSON into the checked-in stats dir with a manifest.

Purpose: The coarse pass is the forcing function for the priority presentation deliverable - it must
land before zoom refinement (03-05) or target adoption (03-06). This plan is deliberately
Claude-driven / human-verified live measurement (D-13); it builds no batch runner.
Output: MCP + servers up, named ladder profiles, coarse raw run JSON + stats manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@.planning/phases/03-baseline-performance-envelope/03-VALIDATION.md
@data/measure/README.md
@.mcp.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install + verify chrome-devtools-mcp (package legitimacy gate) and confirm Toolbox branch</name>
  <files>.mcp.json, .claude/settings.local.json</files>
  <read_first>
    - .mcp.json (existing server registrations to mirror the entry style)
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Package Legitimacy Audit + install steps + Open Q1 branch note)
  </read_first>
  <action>
    Register chrome-devtools-mcp in .mcp.json (pinned version, no @latest) and enable it in
    .claude/settings.local.json enabledMcpjsonServers, gated behind this blocking human-verify per the
    package legitimacy protocol; also confirm the target Toolbox branch for this phase's UI work.
  </action>
  <what-built>
    chrome-devtools-mcp (Google/ChromeDevTools official, researched at v1.4.0) registered in `.mcp.json`
    as `"chrome-devtools": {"command": "npx", "args": ["-y", "chrome-devtools-mcp@<pinned>"]}` with an
    explicitly pinned version (no floating @latest), and enabled locally by adding "chrome-devtools" to
    `enabledMcpjsonServers` in `.claude/settings.local.json`. The package legitimacy audit
    (03-RESEARCH.md) marked it approved (official org) but slopcheck was unavailable, so this install is
    gated behind this blocking human-verify per the graceful-degradation rule.
  </what-built>
  <how-to-verify>
    1. Run `npm view chrome-devtools-mcp version` and confirm the current version; pin that exact version in .mcp.json.
    2. Confirm provenance at https://www.npmjs.com/package/chrome-devtools-mcp - published under the
       ChromeDevTools GitHub org with Google maintainers (google-wombot, orkon, mathias).
    3. Confirm the target Toolbox branch for the D-06/D-11 UI work: local Toolbox is on `data2`; CONTEXT
       cites `data2-research`. State which branch this phase's Toolbox commits land on before proceeding.
    4. Approve the pinned version to be committed to .mcp.json.
  </how-to-verify>
  <verify>
    <automated>node -e "const c=require('/Users/amcclain/dev/hoist-react/.mcp.json'); if(!JSON.stringify(c).includes('chrome-devtools')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.mcp.json` contains a `chrome-devtools` server entry with an explicit pinned version (not `@latest`)
    - `.claude/settings.local.json` lists `chrome-devtools` in `enabledMcpjsonServers`
    - The target Toolbox branch for this phase is recorded in the SUMMARY
    - Human approval recorded before the version is committed
  </acceptance_criteria>
  <resume-signal>Type "approved" with the confirmed version + Toolbox branch, or describe changes.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Launch dev servers in flagged Chrome and define the named ladder scenario profiles</name>
  <files>(no source files - creates ViewManager JsonBlob profiles via the Data Lab UI)</files>
  <read_first>
    - data/measure/README.md (required Chrome launch flags, forced-GC protocol, foregrounded-tab operating note)
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Code Examples: memory rung + CPU rung shapes; Pitfalls 2/3/4/7)
    - data/measure/types.ts (ScenarioConfig / MeasureConfig / DatasetShapeConfig / UpdateConfig fields)
  </read_first>
  <action>
    Launch the Toolbox Grails API + webpack dev server, open a foregrounded flagged Chrome
    (--js-flags="--expose-gc" --enable-precise-memory-info), and author + save the named memory, CPU, and
    anchor ladder profiles listed below via the scenario ViewManager.
  </action>
  <what-built>
    A launched sweep environment and the saved, named ladder profiles the coarse pass runs. To be done
    (Claude may drive via chrome-devtools-mcp in a foregrounded window; otherwise the user runs manually):
    1. Start the Toolbox Grails API (:8080) and webpack dev server via startWithHoist (:3000); poll
       http://localhost:8080/ping and http://localhost:3000/datalab/ until both respond. NOTE: Grails does
       not hot-reload .groovy and a restart logs the user out - no server-side changes are needed here.
    2. Launch flagged Chrome: `--js-flags="--expose-gc" --enable-precise-memory-info` and log in. Keep the
       tab FOREGROUNDED for every run (rAF suspension inflates render - renderSuspect; Pitfall 3).
    3. Author + save (scenario ViewManager) the named ladder profiles. COARSE ladder (Claude's discretion
       point values; zoom in 03-05 adds finer rungs). Shared held-constant knobs: dimensions ['dim0','dim1','dim2'],
       fieldTypeMix {number:5,string:3,date:1,object:1}, seed 0, grid.useVirtualColumns true, DEFAULT_PROTOCOL
       (warmup 5 / measured 20 / gcSettle 50).
       MEMORY ladder (measure {memory:true, performance:false}), name pattern "Mem <leaf> x <fields>f":
         - fieldCount 20 at leafRowCount 5000, 25000, 50000, 100000, 200000
         - leafRowCount 50000 at fieldCount 10, 40  (field-count arm)
       CPU ladder (measure {memory:false, performance:true}) at leafRowCount 50000 / fieldCount 20,
       name pattern "CPU <batch>b <rate>/s <cadence>":
         - batchSize 100, 500, 2000  x  ratePerSec 2, 10  x  cadence steady AND burst
       BASE-03 ANCHOR profile "Anchor 500x20 steady": batchSize 500, breadth 20, fieldCount 20, cadence steady,
         transport webSocket, updateMode incremental, measure {memory:false, performance:true}.
  </what-built>
  <how-to-verify>
    1. Both servers respond (curl :8080/ping and load :3000/datalab/).
    2. Chrome launched with the flags; confirm EnvMetadata on a smoke run shows exposeGc true + preciseMemory true.
    3. The named memory, CPU, and anchor profiles are saved and visible in the scenario ViewManager picker.
  </how-to-verify>
  <verify>
    <human-check>Named ladder profiles saved and selectable; flagged Chrome confirms exposeGc + preciseMemory active</human-check>
  </verify>
  <acceptance_criteria>
    - Grails :8080 and webpack :3000/datalab/ both reachable
    - A smoke run's EnvMetadata shows exposeGc=true and preciseMemory=true (reject the environment otherwise - Pitfall 4)
    - Memory ladder, CPU ladder, and the "Anchor 500x20 steady" profiles are saved as named scenario profiles
  </acceptance_criteria>
  <resume-signal>Type "ready" once servers are up, Chrome is flagged/foregrounded, and the named ladder profiles are saved.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the coarse ladder pass (reference machine) and export raw run JSON to the stats dir</name>
  <files>docs/planning/data2/stats/README.md</files>
  <read_first>
    - data/measure/README.md (forced-GC/median-p95 protocol, renderSuspect discard rule, foregrounded-tab note)
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Pitfalls 1/3/4/5/6; do not read a wall off one rung)
  </read_first>
  <action>
    Run every named memory, CPU (both cadences), and anchor profile on the foregrounded reference machine,
    discarding/re-running any renderSuspect run; export each run to JSON into docs/planning/data2/stats/ and
    write a manifest README naming the reference machine and listing each file's scenario.
  </action>
  <what-built>
    A full COARSE ladder pass on the reference machine, its raw runs exported and checked in:
    1. Run every named memory profile (memory pass) and every named CPU profile (performance pass, both
       cadences) plus the anchor profile. Keep the tab foregrounded; discard and re-run any rung whose run
       carries renderSuspect. Do not trust a single iteration - the protocol already medians over 20.
    2. Optionally drive runs via chrome-devtools-mcp and capture a performance trace on the CPU rungs for
       long-task / dropped-frame corroboration of the D-03 jank wall (treat MCP trace as CORROBORATING only;
       the harness median/p95 is authoritative - Pitfall 5: do not trust the trace's throttle-state summary line).
    3. Export each run to JSON (03-02 export) and save the files under `docs/planning/data2/stats/` with
       filenames encoding shape + cadence. Create `docs/planning/data2/stats/README.md` as a manifest listing
       each file, its scenario, and the NAMED reference machine (model/OS/Chrome version) that produced it.
    OPEN-REPO: no private client names in any stats file or the manifest (Hoist / Toolbox / JobSite only).
  </what-built>
  <how-to-verify>
    1. `docs/planning/data2/stats/` contains one raw JSON per coarse rung (memory + CPU + anchor).
    2. No run in the set has renderSuspect set (re-run any that did).
    3. The manifest names the reference machine and lists each file's scenario.
    4. Spot-check a memory rung shows a positive totalHeapDelta and per-layer breakdown; a CPU rung shows
       engine/genTxn/bridge/render medians+p95.
  </how-to-verify>
  <verify>
    <automated>ls /Users/amcclain/dev/hoist-react/docs/planning/data2/stats/*.json >/dev/null 2>&1 && test -f /Users/amcclain/dev/hoist-react/docs/planning/data2/stats/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/planning/data2/stats/` holds raw run JSON for every coarse memory rung, every CPU rung (steady + burst), and the anchor batch
    - `docs/planning/data2/stats/README.md` exists and NAMES the reference machine + lists each file's scenario
    - No committed stats file contains a forbidden private client name (open-repo guard passes)
    - No run in the checked-in set carries renderSuspect
  </acceptance_criteria>
  <resume-signal>Type "captured" once the coarse ladder raw JSON + manifest are in the stats dir, or report any wall/OOM observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev machine | chrome-devtools-mcp install (supply chain) |
| repo commit -> public repo | stats files could leak private client names (open-repo rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of chrome-devtools-mcp | mitigate | slopcheck + blocking human-verify gate (Task 1); pin exact version, confirm ChromeDevTools org provenance on npmjs.com; audit table in 03-RESEARCH.md |
| T-03-03 | Information disclosure | checked-in stats files + manifest | mitigate | Open-repo rule: no private client names; PreToolUse guard blocks commits with forbidden names; trading-cadence anchor described generically |
</threat_model>

<verification>
- `.mcp.json` contains a pinned chrome-devtools entry
- Coarse stats JSON + manifest present under docs/planning/data2/stats/
- Reference machine named; no renderSuspect runs; no forbidden names
</verification>

<success_criteria>
Sweep environment is up and human-verified, the named ladder profiles exist, and a coarse ladder pass
on the named reference machine is captured as raw JSON in the stats dir - ready to feed the presentation.
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-03-SUMMARY.md` when done.
</output>
