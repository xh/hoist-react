---
phase: 03-baseline-performance-envelope
plan: 07
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-06"]
files_modified:
  - ../toolbox/client-app/src/examples/datalab/DataLabModel.ts
  - ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts
  - ../toolbox/client-app/src/examples/datalab/DataLab.scss
autonomous: true
requirements: [BASE-04]
must_haves:
  truths:
    - "Each targeted PER-SCORECARD metric in the Data Lab scorecard shows an inline pass/fail (or green/yellow/red tier) badge"
    - "The run-comparison table gains target (floor/aspiration) columns and per-metric verdicts"
    - "The two ENVELOPE-LEVEL targets (maxRecordsXFields, sustainedThroughput) render in a distinct envelope-summary area via evaluateEnvelope, not as per-scorecard badges"
    - "Verdicts come from the framework evaluateScorecard + evaluateEnvelope against the adopted DEFAULT_TARGETS"
  artifacts:
    - path: "../toolbox/client-app/src/examples/datalab/DataLabModel.ts"
      provides: "evaluateScorecard wiring + target/verdict projection into comparison rows + envelopeSummary/envelopeVerdicts via evaluateEnvelope"
      contains: "evaluateEnvelope"
    - path: "../toolbox/client-app/src/examples/datalab/DataLabPanel.ts"
      provides: "Scorecard badges + comparison target columns + distinct envelope-targets summary block"
      contains: "badge"
  key_links:
    - from: "DataLabModel"
      to: "@xh/hoist/data evaluateScorecard + evaluateEnvelope + DEFAULT_TARGETS"
      via: "verdicts computed over the run scorecard and the ladder envelope summary against adopted targets"
      pattern: "evaluateEnvelope\\("
---

<objective>
Surface the adopted pass/fail verdicts in the Data Lab UI (D-06): an inline pass/fail (or green/yellow/
red tier) badge next to each PER-SCORECARD metric in the scorecard, and target (floor/aspiration) columns +
verdicts in the run-comparison table - all from the framework `evaluateScorecard` (hoist-react, 03-01)
against the adopted `DEFAULT_TARGETS` (03-06). SEPARATELY, surface the two ENVELOPE-LEVEL targets
(maxRecordsXFields, sustainedThroughput) in a distinct "Envelope targets" summary area computed via
`evaluateEnvelope` over an EnvelopeSummary derived from the whole savedRuns ladder - explicitly NOT as
per-scorecard badges (BASELINE.md, 03-06, is the authoritative record). `DEFAULT_TARGETS` is the single
source of truth, so the presentation export inherits the same verdicts.

Purpose: Completes BASE-04's "wired into the harness as pass/fail criteria" via the app-layer display -
for both the per-scorecard metrics and the envelope-level metrics. Runs last because it consumes both the
framework verdict fns/targets and the adopted numbers.
Output: scorecard badges + comparison target columns + envelope-targets summary in the Data Lab app.

NOTE: This imports NEW hoist-react exports (evaluateScorecard, evaluateEnvelope, TargetsConfig,
EnvelopeSummary, DEFAULT_TARGETS). Toolbox's pre-commit tsc resolves @xh/hoist against the stale installed
package (Phase-2 note), so these symbols may not resolve until hoist-react is re-linked/published. Verify
via the local-hoist-react resolution approach in the verify block, and commit with --no-verify only if the
stale-package tsc is the sole blocker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@.planning/phases/03-baseline-performance-envelope/03-PATTERNS.md
@data/measure/types.ts
@data/measure/ScorecardVerdict.ts

<interfaces>
Consumed from @xh/hoist/data (added in 03-01, adopted numbers in 03-06):
- `evaluateScorecard(sc: Scorecard, targets: TargetsConfig): MetricVerdict[]`
- `evaluateEnvelope(summary: EnvelopeSummary, targets: TargetsConfig): MetricVerdict[]`
- `interface EnvelopeSummary { maxComfortableRecordsXFields: number; maxSustainedThroughput: number; }`
- `interface MetricVerdict { metric; value; floor; aspiration; verdict: 'pass'|'fail'; meetsAspiration; tier: 'comfortable'|'degraded'|'hardWall'; }`
- `const DEFAULT_TARGETS: TargetsConfig`
Data Lab UI seams (from DataLabPanel.ts / DataLabModel.ts):
- `scorecard` factory (line 267) with local `timingRow`/`kv`/`sectionLabel` helpers; `timingRow` (line 351) is designed so "adding a column is just another value"
- `comparisonPanel` factory (line 368) + `comparisonRows` getter (DataLabModel line 470)
Hoist components: `badge` from `@xh/hoist/cmp/badge`; `Icon.checkCircle()`/`Icon.xCircle()` with `intent` - not raw HTML.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire evaluateScorecard + evaluateEnvelope verdicts into DataLabModel</name>
  <files>../toolbox/client-app/src/examples/datalab/DataLabModel.ts</files>
  <read_first>
    - ../toolbox/client-app/src/examples/datalab/DataLabModel.ts (comparisonRows getter, savedRuns, fmtMs/fmtBytes, import block)
    - data/measure/ScorecardVerdict.ts (evaluateScorecard + evaluateEnvelope signatures + MetricVerdict shape)
    - data/measure/types.ts (DEFAULT_TARGETS, MetricVerdict, EnvelopeSummary, Tier)
    - .planning/phases/03-baseline-performance-envelope/03-PATTERNS.md (nullable-pass handling; direct assignment to @bindable)
  </read_first>
  <action>
    Add `evaluateScorecard, evaluateEnvelope, DEFAULT_TARGETS` (and types `MetricVerdict`, `TargetsConfig`,
    `EnvelopeSummary`) to the existing `@xh/hoist/data` import in DataLabModel.ts. Add a computed/getter
    `scorecardVerdicts(sc: Scorecard): MetricVerdict[]` (or a `get lastRunVerdicts()`) that calls
    `evaluateScorecard(sc, DEFAULT_TARGETS)`. Extend `comparisonRows` (line 470) to append target columns per
    metric row: floor, aspiration, and the verdict/tier for each compared run, computed from evaluateScorecard.
    Handle nullable stats exactly as the existing comparisonRows does - only project a verdict for a metric
    whose backing scorecard stat is non-null on that run.
    ENVELOPE-LEVEL: add a `get envelopeSummary(): EnvelopeSummary` that derives the ladder boundary facts from
    `savedRuns` (the largest leaf-records x fields among runs whose per-scorecard metrics are all in the
    comfortable tier; the largest batchSize x ratePerSec among runs that keep up), and a `get envelopeVerdicts():
    MetricVerdict[]` calling `evaluateEnvelope(this.envelopeSummary, DEFAULT_TARGETS)`. These feed the distinct
    envelope-summary UI (Task 2), NOT the per-scorecard badges. Add a small `fmtVerdict`/tier-to-intent helper
    alongside fmtMs/fmtBytes if the badge needs a formatted string or intent name. Use lodash where it reads
    cleaner; `null` over `undefined`; `@action`/`addReaction` for any observable wiring; no em dashes.
  </action>
  <verify>
    <automated>cd /Users/amcclain/dev/toolbox && yarn lint:code; echo "types: run tsc against local hoist-react (see plan note)"; grep -q "evaluateScorecard" client-app/src/examples/datalab/DataLabModel.ts && grep -q "evaluateEnvelope" client-app/src/examples/datalab/DataLabModel.ts</automated>
  </verify>
  <acceptance_criteria>
    - DataLabModel.ts imports `evaluateScorecard`, `evaluateEnvelope`, and `DEFAULT_TARGETS` from `@xh/hoist/data`
    - DataLabModel.ts computes per-metric verdicts via `evaluateScorecard(sc, DEFAULT_TARGETS)`
    - DataLabModel.ts derives an `envelopeSummary` from savedRuns and computes `envelopeVerdicts` via `evaluateEnvelope`
    - `envelopeVerdicts` produces the two envelope-level verdicts (maxRecordsXFields, sustainedThroughput) separately from the per-scorecard badges
    - `comparisonRows` includes floor/aspiration/verdict columns and skips metrics whose backing stat is null
    - `yarn lint:code` exits 0 (tsc verified against local hoist-react per the plan note)
  </acceptance_criteria>
  <done>The model computes framework per-scorecard verdicts and envelope-level verdicts against the adopted targets, projecting the former into comparison rows and exposing the latter for the envelope-summary area.</done>
</task>

<task type="auto">
  <name>Task 2: Render scorecard badges + comparison columns + envelope-targets summary in DataLabPanel</name>
  <files>../toolbox/client-app/src/examples/datalab/DataLabPanel.ts, ../toolbox/client-app/src/examples/datalab/DataLab.scss</files>
  <read_first>
    - ../toolbox/client-app/src/examples/datalab/DataLabPanel.ts (scorecard factory + timingRow helper; comparisonPanel)
    - ../toolbox/client-app/src/examples/datalab/DataLab.scss (existing tb-datalab__* classes for tier colors)
    - cmp/badge/Badge.ts (the Hoist badge component API + intents)
    - .planning/phases/03-baseline-performance-envelope/03-PATTERNS.md (badge component guidance - Hoist badge/Icon, not raw HTML)
  </read_first>
  <action>
    In the `scorecard` factory, add a pass/fail (or green/yellow/red tier) badge cell to the targeted PER-SCORECARD
    metric rows using Hoist `badge` (from `@xh/hoist/cmp/badge`) or an intent-colored `Icon.checkCircle()`/
    `Icon.xCircle()` - NEVER raw HTML. Drive the badge from the model's MetricVerdict (Task 1): intent success
    for comfortable/pass, warning for degraded, danger for hardWall/fail. Extend `timingRow` (or add a verdict
    column) so the badge slots in as "just another value". In `comparisonPanel`, render the new
    floor/aspiration/verdict columns from the extended comparisonRows.
    ENVELOPE-LEVEL: add a distinct "Envelope targets" summary block (e.g. a small labelled section or row group,
    NOT inline per-scorecard badges) that renders the two envelope-level verdicts from `model.envelopeVerdicts` -
    each showing the observed value, floor/aspiration, and a tier badge - with a short note that BASELINE.md is
    the authoritative record for these ladder-evaluated targets.
    Add any needed tier-color classes to DataLab.scss following the existing `tb-datalab__*` naming. Element
    factories only; `Icon.*` for glyphs; no em dashes.
  </action>
  <verify>
    <automated>cd /Users/amcclain/dev/toolbox && yarn lint:code && yarn lint:styles; grep -qi "badge\|checkCircle\|xCircle" client-app/src/examples/datalab/DataLabPanel.ts && grep -qi "envelope" client-app/src/examples/datalab/DataLabPanel.ts</automated>
  </verify>
  <acceptance_criteria>
    - DataLabPanel.ts renders a pass/fail or tier badge per targeted scorecard metric via Hoist `badge` or intent `Icon.*` (grep confirms; no raw `<span class` badge markup)
    - The comparison table shows floor/aspiration/verdict columns
    - A distinct "Envelope targets" area renders the two envelope-level verdicts (maxRecordsXFields, sustainedThroughput) from `envelopeVerdicts`, separate from the per-scorecard metric badges
    - Tier badges map success/warning/danger to comfortable/degraded/hardWall
    - `yarn lint:code` and `yarn lint:styles` exit 0
  </acceptance_criteria>
  <done>The Data Lab scorecard + comparison table display per-scorecard verdicts as badges/columns, and a distinct envelope-targets area displays the two ladder-evaluated envelope verdicts, all inheriting the framework verdict source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | App-layer display of already-computed verdicts over local run data; no new I/O or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | (none material) | scorecard badges / comparison columns / envelope summary | accept | Pure display of local RunResult verdicts; imported-run data already shape-validated at the 03-02 import guard; no untrusted input crosses a new boundary here |

No package installs; no legitimacy gate.
</threat_model>

<verification>
- Toolbox `yarn lint:code` + `yarn lint:styles` clean; tsc verified against local hoist-react
- Scorecard shows per-metric verdict badges; comparison table shows target columns; envelope-targets area shows the two envelope verdicts
- Badges use Hoist badge/Icon, not raw HTML
</verification>

<success_criteria>
Adopted-target pass/fail verdicts render as scorecard badges + comparison target columns (per-scorecard) AND
a distinct envelope-targets summary (max-shape + sustained-throughput) in the Data Lab app, computed from the
framework evaluateScorecard + evaluateEnvelope against DEFAULT_TARGETS (BASE-04 wired end-to-end).
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-07-SUMMARY.md` when done.
</output>
</content>
