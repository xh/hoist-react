---
phase: 03-baseline-performance-envelope
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - docs/planning/data2/stats/README.md
autonomous: false
requirements: [BASE-01, BASE-02]
must_haves:
  truths:
    - "A named small-heap reference machine is defined and its heap cap validated to OOM near the cap"
    - "A small-heap memory pass identifies the degraded->OOM transition (BASE-01 red tier)"
    - "Zoom rungs added around the coarse-pass degradation points sharpen the memory and jank walls"
  artifacts:
    - path: "docs/planning/data2/stats/README.md"
      provides: "Manifest updated with small-heap + zoom run files and the NAMED small-heap machine"
      contains: "small-heap"
  key_links:
    - from: "small-heap + zoom runs"
      to: "docs/planning/data2/stats/"
      via: "Data Lab export (03-02) -> checked-in raw run JSON with EnvMetadata"
      pattern: "datalab-run"
---

<objective>
Sharpen the envelope after the coarse pass: (1) decide + name the small-heap reference machine and
validate the emulated V8 heap cap actually OOMs near the cap (BASE-01); (2) run the small-heap memory
pass to observe the degraded->OOM transition (D-02 red tier) and add zoom rungs around wherever the
coarse pass showed degradation (D-01 zoom), on both memory (leaf x fields) and CPU (batch x rate,
steady + burst) walls. All runs export to the stats dir carrying their own EnvMetadata so cross-machine
comparison never loses the origin machine.

Purpose: Turns the coarse walls into defensible, sharpened wall/tier boundaries that feed the target
proposal (03-06). Runs AFTER the presentation ships (03-04) so it never gates the priority deliverable.
Output: small-heap + zoom raw run JSON in the stats dir; updated manifest naming the small-heap machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-baseline-performance-envelope/03-CONTEXT.md
@.planning/phases/03-baseline-performance-envelope/03-RESEARCH.md
@data/measure/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide + name the small-heap reference machine and validate the heap cap</name>
  <files>(no source files - decision + live validation)</files>
  <read_first>
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Small-Heap Reference Machine section; Pitfalls 1/2/7; Assumptions A1/A3; Open Q2/Q3)
    - data/measure/README.md (Chrome flags; the memory pass's N=50000 per-record sizing that may OOM under a cap)
  </read_first>
  <action>
    Choose the small-heap reference machine (emulated V8 old-space cap vs real hardware), launch it via
    --js-flags="--expose-gc --max-old-space-size=N" --enable-precise-memory-info, and validate live that OOM
    occurs near the cap (A1); record the chosen machine name, the OOM-as-red-tier policy, and whether the
    memory-pass sizing N needs a small-heap override.
  </action>
  <decision>
    What defines the NAMED small-heap reference machine (BASE-01) and does a deliberate OOM count as the
    hard-wall (red) data point vs a discarded failed run?
  </decision>
  <context>
    Past OOM crashes were on older small-heap Chrome machines (PROJECT.md). RESEARCH recommends
    constrained-heap emulation on the dev machine via a V8 old-space cap, cross-validated against real
    older hardware if available. The cap MUST ride inside `--js-flags` alongside `--expose-gc` (Pitfall 2:
    passing `--max-old-space-size` directly to Chrome does nothing). The N=50000 per-record sizing load may
    itself OOM under a small cap before the scenario heap is measured (Pitfall 1/A3).
  </context>
  <options>
    <option id="emulated-512">
      <name>Emulated 512 MB V8 old-space cap on the dev machine (RESEARCH primary)</name>
      <pros>Reproducible, on-demand degraded->OOM transition; no extra hardware; controllable</pros>
      <cons>Emulation, not ground truth; must validate OOM occurs near the cap (A1); sizing N may need scaling down</cons>
    </option>
    <option id="emulated-1024">
      <name>Emulated 1024 MB cap</name>
      <pros>Less likely to OOM during sizing; still constrained</pros>
      <cons>May not represent the smallest real machines that crashed historically</cons>
    </option>
    <option id="real-hardware">
      <name>Real older small-heap Chrome machine (if available), captured via export/import</name>
      <pros>Ground-truth OOM point; calibrates the emulated cap</pros>
      <cons>Availability; less reproducible; still want emulation for on-demand runs</cons>
    </option>
  </options>
  <verify>
    <human-check>Chosen cap launched via --js-flags="--expose-gc --max-old-space-size=N" --enable-precise-memory-info; a load-until-OOM probe crashes near the cap, not the machine's real limit (validates A1); OOM-as-red-tier policy recorded</human-check>
  </verify>
  <acceptance_criteria>
    - The small-heap machine is NAMED (e.g. "Emulated <N> MB V8 old-space cap on <dev model/OS/Chrome version>") for the report (BASE-01)
    - A live probe confirms OOM occurs near the cap (A1 validated) - or the real machine's ground-truth OOM point is recorded
    - The OOM-as-hard-wall vs discarded-run policy is decided and recorded
    - Whether the memory-pass sizing N needs a small-heap override is decided (Pitfall 1/A3)
  </acceptance_criteria>
  <resume-signal>Select: emulated-512, emulated-1024, or real-hardware (with the confirmed OOM behavior + sizing decision).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the small-heap memory pass + zoom rungs; export to the stats dir</name>
  <files>docs/planning/data2/stats/README.md</files>
  <read_first>
    - docs/planning/data2/stats/README.md (existing manifest + coarse runs to zoom around)
    - docs/planning/data2/stats/envelope-stats.json (coarse tier boundaries indicating where to zoom)
    - .planning/phases/03-baseline-performance-envelope/03-RESEARCH.md (Pitfalls 1/3/4/6; keep tab foregrounded; honor EnvMetadata cross-machine)
  </read_first>
  <action>
    Run the memory ladder on the chosen small-heap machine to find the degraded->OOM transition (scaled
    sizing N if Task 1 decided so), add finer zoom rungs (memory + CPU, steady + burst) around the coarse
    degradation points and save them as named profiles, then export all runs to docs/planning/data2/stats/
    and update the manifest README to name the small-heap machine and label zoom/small-heap/coarse runs.
  </action>
  <what-built>
    Sharpened wall data, exported and checked in:
    1. SMALL-HEAP memory pass: on the machine chosen in Task 1, run the memory ladder (scaled sizing N if
       Task 1 decided so) to find the degraded->OOM transition. If a rung OOMs cleanly near the cap, record
       that shape as the red-tier (hard-wall) data point per the Task 1 policy rather than treating it as a
       failed run. Every run carries its own EnvMetadata (the small-heap machine name) so cross-machine
       comparison never loses origin (Pitfall 6).
    2. ZOOM rungs: add finer memory rungs (leafRowCount x fieldCount) and CPU rungs (batchSize x ratePerSec,
       steady + burst) around wherever the coarse pass (03-04 tierBoundaries) showed degradation, to sharpen
       the green->yellow and yellow->red boundaries and the jank wall. Save these as named ViewManager
       profiles too (D-01 reproducibility). Keep the tab foregrounded; discard/re-run any renderSuspect run.
    3. Export all runs to JSON under `docs/planning/data2/stats/` and UPDATE the manifest README to list the
       new files, the NAMED small-heap machine, and which rungs are zoom vs small-heap.
    OPEN-REPO: no private client names in any stats file or the manifest.
  </what-built>
  <how-to-verify>
    1. The stats dir contains small-heap run JSON (each stamped with the small-heap EnvMetadata) and zoom run JSON.
    2. The manifest names the small-heap machine and distinguishes zoom vs small-heap vs coarse runs.
    3. The degraded->OOM transition is observable in the small-heap memory series (a red-tier data point exists).
    4. No renderSuspect runs in the checked-in set; no forbidden client names.
  </how-to-verify>
  <verify>
    <automated>grep -qi "small-heap" /Users/amcclain/dev/hoist-react/docs/planning/data2/stats/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Small-heap run JSON present, each carrying the small-heap machine EnvMetadata
    - Zoom rungs added around the coarse degradation points (memory + CPU) and saved as named profiles
    - The manifest names the small-heap machine and labels zoom/small-heap/coarse runs
    - A red-tier (hard-wall/OOM) memory data point is captured (or documented as not reached at the cap)
    - No renderSuspect runs and no forbidden client names in the committed set
  </acceptance_criteria>
  <resume-signal>Type "captured" with the small-heap wall + zoom findings, or report any anomaly (e.g. OOM during sizing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit -> public repo | Stats files could leak private client names (open-repo rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information disclosure | checked-in small-heap/zoom stats + manifest | mitigate | Open-repo rule: no private client names; PreToolUse guard on commit |
| T-03-05b | Reliability (measurement validity) | emulated heap cap correctness | mitigate | Live validation that OOM occurs near the cap (A1); reject small-heap numbers if the cap did not take effect (Pitfall 2/7); honor EnvMetadata across machines (Pitfall 6) |

No package installs; no legitimacy gate (chrome-devtools-mcp gated in 03-03).
</threat_model>

<verification>
- Small-heap + zoom run JSON in stats dir, each with correct EnvMetadata
- Manifest names the small-heap machine; distinguishes run families
- Heap cap validated to OOM near the cap; no renderSuspect; no forbidden names
</verification>

<success_criteria>
The memory and jank walls are sharpened by zoom rungs and the small-heap machine's degraded->OOM
transition is captured under a named, cap-validated machine - the evidence base for target adoption.
</success_criteria>

<output>
Create `.planning/phases/03-baseline-performance-envelope/03-05-SUMMARY.md` when done.
</output>
